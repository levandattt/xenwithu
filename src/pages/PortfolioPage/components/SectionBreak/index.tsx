import React from 'react';
interface HeaderProps {
  
}

const SectionBreak: React.FC<HeaderProps> = () => (
    <div className={`py-3 px-10 flex items-center justify-between max-md:justify-center`}>
   
    </div>
);

export default SectionBreak;