import React from 'react';
import BrandLogo from '../BrandLogo';
interface CardHorizontalProps {
    children?: React.ReactNode;
}

const CardHorizontalItem: React.FC<CardHorizontalProps> = (props) => (
    <></>
);

export default CardHorizontalItem;